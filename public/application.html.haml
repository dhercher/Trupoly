!!!
%html
  %head
    %title Trupoly
    = stylesheet_link_tag    "application"
    = javascript_include_tag "application"
    = csrf_meta_tags
  - flash.each do |name, msg|
    - if msg.is_a?(String)
      = content_tag :div, msg, :id => "flash_#{name}"
  .bg_photo= image_tag 'street_view.jpg', width: '200%' 
  %body
    - if @style_sheets != 'sessions'
      = button_to 'LOGOUT', '/logout', class:'button logout_button', :method => :get
    = render 'layouts/header'
    = yield
    = render 'layouts/footer'